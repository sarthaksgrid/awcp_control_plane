"""
AWCP — Shared UI Components
===============================
Reusable Streamlit components used across all views:

  - kpi_card()           — single KPI metric display
  - kpi_grid()           — row of KPI cards
  - workflow_table()     — sortable workflow queue table
  - tool_gate_table()    — action gate status table
  - evidence_code_block() — syntax-highlighted evidence display
  - degradation_ladder() — accordion showing autonomy steps
  - status_badge()       — colored status indicator
  - feedback_carousel()  — rotating telemetry cards
  - sidebar_nav()        — navigation sidebar
"""
