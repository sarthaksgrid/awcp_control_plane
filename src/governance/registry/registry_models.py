"""
AWCP — Agent Registry Database Models
=======================================
SQLAlchemy ORM models for the agent registry tables:

  - agents               — core agent catalog
  - agent_write_scopes   — declared write scopes per agent
  - agent_hooks          — control hook status (telemetry, flags, policy)
  - agent_audit_log      — version history of registry changes
"""
