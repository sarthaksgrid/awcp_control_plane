"""
AWCP — Database Engine & Session Management
=============================================
SQLAlchemy async engine setup, session factory,
and base declarative model for all ORM tables.

Covers:
  - create_engine()       — async PostgreSQL engine
  - get_session()         — async session dependency for FastAPI
  - Base                  — declarative base for all models
"""
