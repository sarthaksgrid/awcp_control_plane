# ============================================================
# AWCP — API Dockerfile
# ============================================================
# Multi-stage build for the FastAPI control plane API.
#
# Stages:
#   1. builder  — install dependencies
#   2. runtime  — slim image with app code
#
# Exposes port 8000.
# ============================================================
